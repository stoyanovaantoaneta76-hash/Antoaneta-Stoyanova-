name: Nordlys Core Dev

on:
  push:
    branches: [dev]
    paths:
      - 'nordlys-core/**'
      - '.github/workflows/nordlys-core-ci-dev.yml'
  pull_request:
    branches: [dev]
    paths:
      - 'nordlys-core/**'
      - '.github/workflows/nordlys-core-ci-dev.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: linux, runner: ubuntu-24.04 }
          - { os: macos, runner: macos-15 }

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cache Conan packages
        uses: actions/cache@v5
        with:
          path: ~/.conan2
          key: conan-${{ runner.os }}-${{ hashFiles('nordlys-core/conanfile.py') }}
          restore-keys: |
            conan-${{ runner.os }}-

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install build tools
        run: uv pip install conan --system

      - name: Setup Conan profile
        run: conan profile detect --force

      - name: Install dependencies
        working-directory: nordlys-core
        run: conan install . --build=missing -s compiler.cppstd=20

      - name: Configure
        working-directory: nordlys-core
        shell: bash
        run: |
          cmake --preset conan-release \
            -DNORDLYS_BUILD_TESTS=ON \
            -DNORDLYS_BUILD_PYTHON=ON \
            -DNORDLYS_BUILD_C=ON \
            -DNORDLYS_BUILD_BENCHMARKS=ON \
            -DNORDLYS_ENABLE_CUDA=OFF \
            -DNORDLYS_ENABLE_TRACY=OFF

      - name: Build
        working-directory: nordlys-core
        run: cmake --build --preset conan-release --parallel

      - name: Test (C++ only)
        working-directory: nordlys-core
        run: ctest --preset conan-release --output-on-failure -E python_bindings

      - name: Run Benchmarks
        working-directory: nordlys-core
        run: |
          ./build/Release/benchmarks/bench_nordlys_core \
            --benchmark_format=json \
            --benchmark_out=benchmark_results.json

      - name: Download previous benchmark data
        uses: actions/cache@v5
        with:
          path: ./cache
          key: ${{ runner.os }}-benchmark-nordlys-core

      - name: Store and Compare Benchmark Results
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: Nordlys Core Benchmarks (${{ matrix.os }})
          tool: 'googlecpp'
          output-file-path: nordlys-core/benchmark_results.json
          external-data-json-path: ./cache/benchmark-data.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          alert-threshold: '150%'
          comment-on-alert: true
          fail-on-alert: false

      - name: Upload Benchmark Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: benchmark-results-${{ matrix.os }}
          path: nordlys-core/benchmark_results.json
          retention-days: 90

  # ============================================
  # Python Bindings Tests
  # ============================================
  python-bindings-test:
    name: Python Bindings (${{ matrix.os }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: linux, runner: ubuntu-24.04 }
          - { os: macos, runner: macos-15 }

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cache Conan packages
        uses: actions/cache@v5
        with:
          path: ~/.conan2
          key: conan-${{ runner.os }}-${{ hashFiles('nordlys-core/conanfile.py') }}
          restore-keys: |
            conan-${{ runner.os }}-

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install build tools
        run: uv pip install conan --system

      - name: Install Python test dependencies
        working-directory: nordlys-core/bindings/python
        run: uv sync --group test --no-install-project

      - name: Setup Conan profile
        run: conan profile detect --force

      - name: Install dependencies
        working-directory: nordlys-core
        run: conan install . --build=missing -s compiler.cppstd=20

      - name: Configure
        working-directory: nordlys-core
        shell: bash
        run: |
          cmake --preset conan-release \
            -DNORDLYS_BUILD_TESTS=OFF \
            -DNORDLYS_BUILD_PYTHON=ON \
            -DNORDLYS_BUILD_C=OFF \
            -DNORDLYS_BUILD_BENCHMARKS=OFF \
            -DNORDLYS_ENABLE_CUDA=OFF \
            -DNORDLYS_ENABLE_TRACY=OFF

      - name: Build Python bindings
        working-directory: nordlys-core
        run: cmake --build --preset conan-release --parallel --target nordlys_core_ext

      - name: Run Python tests
        working-directory: nordlys-core/bindings/python
        run: |
          uv run pytest tests/ -v --tb=short

  # ============================================
  # Build Wheels (CPU and CUDA in parallel)
  # ============================================
  build_wheels_cpu:
    uses: ./.github/workflows/_build-wheels-cpu.yml
    with:
      validate-version: false

  build_wheels_cuda:
    uses: ./.github/workflows/_build-wheels-cuda.yml
    with:
      validate-version: false
